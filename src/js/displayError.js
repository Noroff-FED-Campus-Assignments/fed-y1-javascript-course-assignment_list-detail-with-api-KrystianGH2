export function displayErrorMessage( message = "OH SHOOT! FAILED FETCHING CHARACTERS FROM RICK AND MORTY API"){
    return `<div class="error">${message}</div>`;

}

export function displayError( message = "OH SHOOT! FAILED FETCHING CHARACTERS FROM RICK AND MORTY API"){
    return `<div class="error">${message}</div>`;

}

